you need to have installed node:
you can install it here: https://nodejs.org/en

you need these dependencies insatlled:
axios -> npm install axios

for normal web-crawl that scans the html file for any anchor tags use:
node web-crawl.js

for brute-force web that uses common.txt wordlist from secLists use:
node web-crawl.js --bruteforce
